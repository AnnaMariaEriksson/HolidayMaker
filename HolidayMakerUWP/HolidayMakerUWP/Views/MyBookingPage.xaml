<Page x:Class="HolidayMakerUWP.Views.MyBookingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:HolidayMakerUWP.Views"
      xmlns:local1="using:HolidayMakerUWP.Model"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition />
                <RowDefinition Height="6*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*" />
                <ColumnDefinition Width="7*" />
                <ColumnDefinition Width="1*" />
            </Grid.ColumnDefinitions>

            <Grid Grid.Row="1"
                  Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition />
                    <RowDefinition Height="14*" />
                </Grid.RowDefinitions>
                <StackPanel>
                    <TextBlock Text="Vald bokning"
                               FontSize="40"
                               HorizontalAlignment="Center" />

                    <ListView HorizontalAlignment="Center">
                        <ListViewItem>
                            <DataTemplate x:DataType="local1:Room">
                                <StackPanel>
                                    <TextBlock Text=""/>
                                </StackPanel>
                            </DataTemplate>
                        </ListViewItem>
                    </ListView>
                </StackPanel>

            </Grid>
            <StackPanel Grid.Row="1"
                        Orientation="Vertical">
                <TextBlock Text="Dina Bokningar"
                           FontSize="40"
                           HorizontalAlignment="Center" />
                <ListView VerticalAlignment="Stretch"
                          ItemsSource="{x:Bind myBookingPageViewModel.NewBookings}"
                          Height="300">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local1:Booking">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="20">
                                <TextBlock Text="{x:Bind StartDate.UtcDateTime}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
                <TextBlock Text="Boknings historik"
                           FontSize="40"
                           HorizontalAlignment="Center" />
                <ListView ItemsSource="{x:Bind myBookingPageViewModel.OldBookings}">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local1:Booking">
                            <StackPanel>
                                <TextBlock Text="{x:Bind StartDate.UtcDateTime}" />
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
