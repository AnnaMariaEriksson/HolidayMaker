<Page x:Class="HolidayMakerUWP.Views.BookingPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:local="using:HolidayMakerUWP.Views"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="6*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="8*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid Grid.Row="1"
              Grid.Column="1">
            <Grid.RowDefinitions>
                <RowDefinition Height="*" />
                <RowDefinition Height="5*" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>
            <TextBlock Grid.Row="0"
                       Grid.Column="1"
                       Text="Bokning"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Top"
                       FontSize="56" />

            <Button Grid.Row="2"
                    Grid.Column="1"
                    HorizontalAlignment="Center"
                    VerticalAlignment="Center"
                    Content="Boka (Totalpris)"
                    Width="350"
                    Height="70"
                    FontSize="40"
                    CornerRadius="25" />
            <ListView Grid.Row="1"
                      Grid.Column="0"
                      BorderThickness="3"
                      BorderBrush="Black"
                      Width="700"
                      HorizontalAlignment="Left"
                      VerticalAlignment="Stretch"
                      Margin="5">
            </ListView>
            <StackPanel Orientation="Horizontal"
                        Grid.Row="1"
                        Grid.Column="1"
                        HorizontalAlignment="Left"
                        >
                <StackPanel Orientation="Vertical"
                            Grid.Row="1"
                            Grid.Column="1"
                            HorizontalAlignment="Center">
                    <TextBlock Text="Namn" FontSize="24"/>
                    <TextBlock Text="Efternamn" FontSize="24"/>
                </StackPanel>
                <TextBlock Text="Johan Svensson" FontSize="24"/>
                <StackPanel>

                </StackPanel>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
